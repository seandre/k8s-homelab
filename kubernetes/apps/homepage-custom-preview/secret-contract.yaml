apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-cross-cluster-secret-contract
  namespace: homepage
  labels:
    app.kubernetes.io/name: homepage-custom
data:
  # Values are deliberately absent. The workload requires this manually
  # managed Secret before either Deployment can become Ready.
  homepage-okd-api: server,token
