apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-custom-preview
  namespace: homepage
  labels:
    app.kubernetes.io/name: homepage-custom
    app.kubernetes.io/instance: preview
    app.kubernetes.io/component: dashboard
data:
  runtime-mode.json: |
    {"mode":"production","dataMode":"live","integrations":"approved-read-only"}
